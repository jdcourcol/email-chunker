[project]
name = "email-chunker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pgvector>=0.4.1",
    "transformers>=4.55.4",
]
